<nav class="navbar navbar-expand-lg bg-body-tertiary bg-dark"
    data-bs-theme="dark">
    <div class="container-fluid">
        <% if(locals.user){ %>
        <a class="navbar-brand" href="/"><%= locals.user.name %></a>
        <% } %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
            data-bs-target="#navbarNav" aria-controls="navbarNav"
            aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">

                <% if(locals.user && !locals.user.isAdmin){ %>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page"
                        href="/users/signOut">Log out</a>
                </li>
                <% } else if(locals.user && locals.user.isAdmin){ %>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/admin">Add
                        Task</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/admin/employees">Employees</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/admin/allReviews">Reviews</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/users/signOut">Logout</a>
                </li>
                <% }else{ %>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page"
                        href="/users/signIn">Sign In</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page"
                        href="/users/signUp">Sign Up</a>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>